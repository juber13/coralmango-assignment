// question one
const arr1 = [1, 3, 5, 2, 4];
const res = arr1.length > 0 ? arr1.filter((arr ,index) => index % 2 === 0) : -1;
console.log(res)

//question two
const arr2 = [1, 3, 5, 2, 4];
const res2 = arr2.length > 0 ?  arr2.map(arr => Math.pow(arr, 2)) : -1;
console.log(res2)


//qiestion three
const arr3 = [1, 3, 5, 2, 4];
const res3 = arr3.length > 0 ? arr3.filter((arr , index) =>  index % 2 === 0).map(arr => Math.pow(arr , 2)) : -1;
console.log(res3)